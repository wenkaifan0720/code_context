name: code_context
description: Lightweight semantic code intelligence. Query your codebase with a simple DSL.
version: 0.2.0

environment:
  sdk: ^3.5.0

# Pub workspace configuration
workspace:
  - packages/scip_server
  - packages/dart_binding

dependencies:
  # Workspace packages
  scip_server:
  dart_binding:

  # MCP server support
  dart_mcp: ^0.3.3
  
  # Utilities
  path: ^1.8.0
  args: ^2.4.0

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
